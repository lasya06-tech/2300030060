//if database is used by millions of users,
we may get stuck at some point

even hotstar manages so many crores of people while matching matches.
how it is possible?
(hotstar follows content-delivery-network) 
which the content is already stored in nearby locations of users,
later the content is broken into chunks and get delivered.

-->So burden dont goes to original server.

-->We can implement the same thing if millions of usres are accessing our website.

